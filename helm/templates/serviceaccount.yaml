apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ .Chart.Name }}
  labels:
    app: {{ .Chart.Name }}
  {{- if .Values.aws.secretsManager.enabled }}
  annotations:
    eks.amazonaws.com/role-arn: {{ .Values.aws.serviceAccount.roleArn }}
  {{- end }}